<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Buttonが押された時 -->
    <item android:state_pressed="true" android:color="@color/olive"/>
    <!-- android:drawable="@drawable/start_button_clicked" -->
    <!-- 色を変更する場合は、android:color="@color/red" のようにする -->


    <!-- Buttonが押されてない時 -->
    <item  android:color="@color/white" android:state_pressed="false"/>

    <!-- Buttonにフォーカスがあたった時 -->
    <item android:color="@color/olive" android:state_focused="true"/>
    <item></item>

</selector>